# Переменные для подстановки в alg.py
# Используются во всех функциях при заполнении/проверке данных

# === Источник данных (верхний уровень) ===
# Файл xlsx с отчётом (load_from_xlsx)
xlsx_file = "27022026.xlsx"

# === search_case(number_case, cooldown) — поиск дела ===
# Параметр при вызове:
number_case =   # номер/имя дела для поиска в 1С (аргумент при вызове search_case)

# === handle_court() — вкладка СУД ===
name_defedant = Козулин Владислав Валерьевич   # Ответчик (ФИО)
court = Свердловский районный суд г. Красноярска   # Суд (название)
date_base = 21.10.2024         # Дата 1
date_plus_mounth = 05.09.2024  # Дата 2
result_case = удч              # Результат по делу: удп | удч | отк (удп — полностью, удч — частично, отк — отказ в иске)
summ_requests_s = 400000      # Запрошенная сумма (страховое возмещение)
summ_real_s = 400000          # Одобренная сумма (страховое возмещение)
summ_requests_g = 7200        # Запрошенная сумма (госпошлина)
summ_real_g = 7200            # Одобренная сумма (госпошлина)

# Коды видов выплат в 1С (если понадобится менять):
# ЦБД000031 — страховой платёж (страховое возмещение)
# ЦБД000016 — госпошлина

# === ip_handler() — вкладка ИП (исполнительное производство) ===
view_ip_list = Оригинал исполнительного листа   # Вид исполнительного листа (один из ключей ip_list_map)
number_ip_list = ФС №043214528                  # Номер исполнительного листа
summ = 407200.24                               # Сумма
data_get_ip_list = 10.01.2025                  # Дата

# Варианты view_ip_list (ip_list_map):
# Оригинал исполнительного листа
# Постановление ФССП
# Инкассове списание из банка
# Дубликат испольнительного листа
# Добровольная оплата по решению суда
